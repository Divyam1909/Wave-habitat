// Full Code for: app/(auth)/login/page.tsx

import { LoginForm } from "@/components/auth/login-form";

export default function LoginPage() {
  // This page's only job is to render the form.
  // The layout is handled by the parent layout.tsx file.
  return <LoginForm />;
}